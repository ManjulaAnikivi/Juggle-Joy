@model juggle_joy.Models.HandlerAssistantValidation
@{
	Layout = null;
}
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@MvcApplication.project_name</title>

    <link rel="stylesheet" href="~/Assets/css/dashboard.css">
    @Html.Partial("assistantHandler_css")
</head>

<body class="body-my-profile-page">
	@Html.Partial("handlerheader");

	<main class="main my-profile-page">
		<div class="dashboard-background">

			<section class="my-profile">
				<div class="my-profile-main">
					<div class="container">
						<div class="content-wrapper">
							<div class="row">
								@Html.Partial("handlersidebar")
								<div class="col-xl-9 col-lg-9 col-md-12 col-12 main-content">
									@if (@Model.UserID > 0)
									{
										<h2>
											Edit Assistant
										</h2>
									}
									else
									{
										<h2>
											Add Assistant
										</h2>
									}

									<p id="lbl_msg" style="color: #00c1ab">@ViewBag.msg</p>
									@using (Html.BeginForm("add_assistants", "Handlers", FormMethod.Post, new { @id = "profile_form", @name = "profile_form" }))
									{
										@Html.HiddenFor(x => x.UserID)
										<fieldset>
											<div class="half-fields">
												<div class="form_field">
													<label for="FirstName">First Name*</label>
													@Html.TextBoxFor(x => x.FirstName, "", new { @placeholder = "First Name" })
													@Html.ValidationMessageFor(x => x.FirstName, "", new { @class = "text-danger" })

												</div>
												<div class="form_field">
													<label for="LastName">Last Name*</label>
													@Html.TextBoxFor(x => x.LastName, "", new { placeholder = "Last Name" })
													@Html.ValidationMessageFor(x => x.LastName, "", new { @class = "text-danger" })
												</div>
											</div>

											<div class="half-fields">
												<div class="form_field">
													<label for="EmailID">Email ID*</label>
													@Html.TextBoxFor(x => x.EmailID, "", new { @placeholder = "EmailID" })
													@Html.ValidationMessageFor(x => x.EmailID, "", new { @class = "text-danger" })
												</div>
												<div class="form_field">
													<label for="UserName">User Name*</label>
													@Html.TextBoxFor(x => x.UserName, "", new { @placeholder = "User Name" })
													@Html.ValidationMessageFor(x => x.UserName, "", new { @class = "text-danger" })
												</div>
											</div>
											<div class="half-fields single-field">
												<div class="form_field">
													<label for="PasswordHash">Password*</label>
													@Html.TextBoxFor(x => x.PasswordHash, "", new { @placeholder = "Password", @readonly = "readonly", @Value = "Juggle@100", style = "opacity:0.6" })
													@*@Html.ValidationMessageFor(x => x.PasswordHash, "", new { @class = "text-danger" })*@
												</div>
											</div>
											<div class="form_submit">
												<input type="submit" value="Save">
												<button type="button" class="cancel-btn">Cancel</button>
											</div>
										</fieldset>
									}

								</div>
							</div>
						</div>
					</div>
				</div>
			</section>

		</div>
	</main>

	@Html.Partial("handlerfooter")

	@Html.Partial("js")

	<script>

		if ('@ViewBag.msg' == null) {
			$("#lbl_msg").fadeOut();
		}
		else {
			$("#lbl_msg").fadeOut(5000);

		}

		$("#ink_assistant").addClass("active");
		$(".cancel-btn").on("click", function () {
			window.location.href = "/add-assistants";
		})
	</script>

	<script src="~/Assets/js/jquery.custom.selectbox.min.js"></script>

</body>

</html>