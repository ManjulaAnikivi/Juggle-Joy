
@using juggle_joy.Models
@{
    db_jugglejoyEntities ctx = new db_jugglejoyEntities();
    int handlerID = Convert.ToInt32(Session["HandlerID"]);
    //var notifications = ctx.tbl_notification.Where(x => x.ReadStatus == "0" && x.ToID == handlerID).OrderByDescending(x => x.NotificationID).ToList().Take(5);
    //var notify_count = ctx.tbl_notification.Where(x => x.ReadStatus == "0" && x.ToID == handlerID).OrderByDescending(x => x.NotificationID).ToList();
    //var notifications = notificationsa.GroupBy(x => x.Url).Take(5).ToList();
    //var groupedCustomerList = ctx.tbl_notification
    //.GroupBy(x => x.Url)
    //.Select(grp => grp.ToList())
    //.ToList().Take(5);
}

<style>
    .msgNotification {
        color: white;
        font-size: 16px !important;
        display: block;
        background: rgb(0, 193, 171);
        opacity: 0.7;
        display: flex;
        align-items: center;
        gap: 20px;
        font-size: 14px;
        font-weight: 400;
        line-height: 20px;
        border: 1px solid var(--secondary-color);
        border-radius: 10px;
        padding: 14px 20px;
        margin: auto;
        max-width: 466px;
    }
</style>
<header class="secondary-header admin-header">
    <div class="header-main">
        <div class="container-fluid">
            <div class="header-wrapper">
                <div class="logo">
                    <a href="/handler-dashboard">
                        <img src="~/Assets/images/logo.png" alt="Jugglejoy-logo">
                    </a>
                </div>

                <div class="admin-header-wrapper">
                    <div class="header-search-box">
                        <div class="header-search-box-container">
                            <form action="/Handlers/tasks_searchlist" method="get">
                                <div class="header-search-box-wrapper">
                                    <input type="text" name="keywords" placeholder="Search keyword here...">
                                    <button class="admin-header-search-btn">
                                        <i class="fa-solid fa-magnifying-glass"></i>
                                    </button>
                                </div>
                            </form>
                        </div>
                    </div>

                    <div class="search-box-mobile-btn">
                        <button><i class="fa-solid fa-magnifying-glass"></i></button>
                    </div>
                    <div class="chat" style=" width: 36px !important;">

                    </div>
                    <div class="admin-header-notification">


                    </div>

                    @if (Session["HandlerID"] != null)
                    {
                        <div class="avatar-main">
                            <div class="avatar-logo">
                                <a href="javascript:void(0)">
                                    <!-- <img src="~/Assets/images/logo-100x100.png" alt="logo"> -->
                                    <span> @Session["HandlerFirstName"]@Session["HandlerLastName"]</span>
                                </a>

                                <div class="submenu">
                                    <!-- <div class="popup-cross-btn">
                        <button><i class="fa-solid fa-circle-xmark"></i></button>
                    </div> -->
                                    <ul>
                                        @*<li><a href="/add-assistants">Add Assistant</a></li>*@
                                        <li><a href="/handler-dashboard">Dashboard</a></li>
                                        <li><a href="/handler-profile">Update Profile</a></li>
                                        @*<li><a href="/handler-change-password">Change Password</a></li>*@
                                        <li><a href="/Handlers/handler_logout">Logout</a></li>
                                    </ul>
                                </div>
                            </div>
                        </div>
                    }

                </div>
            </div>
        </div>
        <p class="msgNotification" id="lbl_handlerNotification" style="display:none">
            <i class="fa fa-check-circle-o" style="color: white;"></i>You have received new notification
            <span id='pop-off' style=" position: relative; left: 32%; cursor: pointer "> <i class='fa-solid fa-circle-xmark' style="color:white;"></i></span>
        </p>
    </div>
</header>

<script src="~/Scripts/jquery-3.3.1.min.js"></script>
<script>
    $(document).ready(function () {

        //submenu open/close
        $(".avatar-logo a").click(function () {
            $(".submenu").toggleClass("submenu-visible");
        });
        // $(".submenu .popup-cross-btn button").click(function () {
        //     $(".submenu").removeClass("submenu-visible");
        // });
        $(document).click(function (event) {
            if (!$(event.target).closest(".submenu,.avatar-logo a").length) {
                $("body").find(".submenu").removeClass("submenu-visible");
            }
        });

    })

    function ReadStatus(id, Url) {
        debugger
        var Url = Url;
        $.ajax({
            type: "post",
            url: "/Handlers/notification/",
            data: { id: id },
            datatype: "json",
            //traditional: true,
            success: function (data) {
                if (data.success) {
                    window.location.href = Url;
                }
                else if (data.error) {
                    //window.location.href = Request.url;
                }
            },
        });
        return true;
    }
</script>


<script>

    $("#pop-off").on("click", function () {
        debugger
        $("#lbl_handlerNotification").css("display", "none");
    })
</script>


<script src="~/Scripts/jquery.signalR-2.4.3.min.js"></script>
<script src="/signalr/hubs"></script>


<script>
    $(".admin-header-notification").load("/Handlers/handlernotifications", function () { });
    $(".chat").load("/Handlers/chatnotification", function () { });

    jQuery.noConflict();
    // JavaScript to connect to SignalR Hub and handle notifications
    jQuery(document).ready(function ($) {
        $(function () {
            debugger
            var notificationHub = $.connection.myHub;
            if (typeof notificationHub.client != 'undefined') {
                // Assign function to OrderCreatedNotification
                notificationHub.client.SendNotification = function (message, name,id) {
                    //alert(message);
                    //alert(name);
                    if (name == "handler" && id=='@handlerID') {
                        $(".admin-header-notification").load("/Handlers/handlernotifications", function () { });
                        $(".chat").load("/Handlers/chatnotification", function () { });
                        $("#lbl_handlerNotification").css("display", "block");
                        //$("#lbl_handlerNotification").fadeOut(5000);
                        setTimeout(function () {
                            
                            $("#lbl_handlerNotification").fadeOut(5000);
                        }, 5000);
                    }

                };
            } else {
                console.error('notificationHub.client is not defined');
            }

            $.connection.hub.start().done(function () {
                console.log("SignalR connected");
            }).fail(function () {
                console.error("SignalR connection failed");
            });
        });
    });
</script>

